/dist/
/app/dist/
/server/dist/
/app/unpackage/
/project.private.config.json
/node_modules/
/app/node_modules/
/server/node_modules/
/test/node_modules/
.eslintcache
CLAUDE.md
.env*.local
.env.production

# Dependencies
node_modules/
/output/

# Playwright artifacts
/test/test-results/
/test/playwright-report/
/playwright-report/

# Quality-tool reports (regenerable)
/reports/

# perf_baseline_gate.js writes this on every quality run; it is a snapshot,
# not a contract — staging it via the pre-commit `git add -u` step polluted
# diffs with non-declarative changes (see commit ed075a8 / 012270c). The
# gate now writes to reports/ instead, and any pre-existing tracked copy
# under scripts/ should be removed.
/scripts/perf_baseline.json
